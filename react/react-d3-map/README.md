# Interactive Map using D3

Aim - Create an interactive performane oriented map in D3.JS and React-Typescript.

## Setup

- `yarn` to install dependencies
- `yarn dev` to start project
- checkout `package.json` for more commands
